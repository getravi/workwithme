# Release Notes (Historical)

> **This document is preserved for historical reference only.**
> It documented the old Node.js SEA binary bundling process used prior to the Rust backend migration.
>
> **The current release process is in [RELEASING.md](RELEASING.md).**
> The Rust backend is now compiled directly into the Tauri binary — no sidecar or `externalBin` configuration is needed.
